/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {Board} from './src/screens/Board.tsx';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Board);
